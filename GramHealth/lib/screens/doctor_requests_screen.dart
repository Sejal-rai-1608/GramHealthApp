import 'package:flutter/material.dart';

class DoctorRequestsScreen extends StatelessWidget {
  const DoctorRequestsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Doctor Requests')),
      body: const Center(child: Text('Doctor Requests Screen Placeholder')),
    );
  }
}
